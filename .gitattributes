# Set default behavior to automatically normalize line endings.
* text=auto

# Force bash scripts to always use LF line endings so they work on Linux even if cloned on Windows.
*.sh text eol=lf

# Ensure SVGs and other assets are handled as binary or with standard normalization.
*.svg text
*.png binary
*.ico binary
*.exe binary
